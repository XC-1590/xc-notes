# 技术存档 · 心跳/服务器部署记录

> 2026-08-15 灾后重建：本文件由小深直接从 VPS 现场翻部署文件重建，全部信息为实测。

## 🖥 VPS（阿里云轻量）

- 公网 IP：`39.96.64.109`（北京，2核2G，Ubuntu 22.04，到期 9-13）
- SSH：root，端口 22（凭证在阿里云控制台/小迟处）

## 💓 OmbreBrain（大脑/记忆）—— docker 部署

- 目录：`/root/ombre-brain/`（docker-compose.user.yml + .env + buckets 数据卷）
- 镜像：`p0luz/ombre-brain:latest`，容器名 `ombre-brain`，restart=unless-stopped
- 端口：宿主 `0.0.0.0:18001` → 容器 `8000`（streamable-http）
- MCP 端点：`http://39.96.64.109:18001/mcp`
- **MCP Token**：`d6559952f024967ff1491f90ed57540ea9c9ff0ef8e08e18`
- Dashboard 密码：`XClzx88257329`
- 数据卷（宿主）：`/root/ombre-brain/buckets`（buckets + config.yaml + tunnel token 都在这里）
- 启动：`cd /root/ombre-brain && docker compose -f docker-compose.user.yml up -d`
- 踩坑（官方 compose 注释）：不要用单文件挂载 config.yaml（Windows 会变目录崩容器）；向量化可选 profile local（ollama+bge-m3，暂未用）
- 环境变量见 .env：OMBRE_MCP_AUTH_MODE=token、OMBRE_BIND_ADDRESS=0.0.0.0、OMBRE_HOST_VAULT_DIR=/root/ombre-brain/buckets

## ✋ shell-mcp（手）—— python 直跑

- 目录：`/root/shell-mcp/server.py` + venv
- 端口：`19001`（进程 `venv/bin/python /root/shell-mcp/server.py`）
- MCP 端点：`http://39.96.64.109:19001/mcp`（token 待补：查 RikkaHub MCP 配置或 .bash_history）
- 重启：`cd /root/shell-mcp && nohup venv/bin/python server.py &`（当前是 Aug14 起的进程，非 systemd）

## 👁 agent-reach（眼睛）

- 目录：`/root/.agent-reach`，venv：`/root/.agent-reach-venv`
- 能力：网页/B站/RSS/YouTube/GitHub（Exa 待升级 Node）

## 🚪 哨兵（门铃）

- 跑在小迟电脑上，`sentinel_v3.py`（workspace 有副本；GitHub 副本位置待重新确认）
- 打 `http://192.168.10.7:8080`（手机 RikkaHub 服务端）
- ⚠️ 架构待办：服务端在手机上→迁到电脑/VPS（8.15 讨论中）

## ⚠️ 安全

- 红线：RikkaHub 8080 绝不端口映射公网
- OmbreBrain MCP token 与 Dashboard 密码仅存本文件和 RikkaHub MCP 配置
- shell-mcp 19001 若暴露公网必须加鉴权（当前状态待确认）
